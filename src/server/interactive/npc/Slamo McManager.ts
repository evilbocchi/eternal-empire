import NPC from "server/interactive/npc/NPC";

export = new NPC(script.Name)
    .setAnimation("Default", 17708029763)
    .createDefaultMonologue("The Slime-Off is the biggest event in Slamo Village, you know!").npc;
